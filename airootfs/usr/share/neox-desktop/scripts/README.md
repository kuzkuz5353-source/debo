# NEOX scripts

- `install.sh`: system/user installation helper.
- `neox-session.sh`: display-manager session entry point.
- `neox-autostart.sh`: line-based autostart runner.
- `uninstall.sh`: removes installed NEOX files.
