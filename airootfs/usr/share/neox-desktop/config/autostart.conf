# NEOX user autostart commands.
# Lines are executed by neox-shell; empty lines and comments are ignored.

# Apply saved theme at login.
neox-theme-engine neox-hd

# Configure file manager integration.
neox-file-manager-integration --configure

# Optional examples:
# blueman-applet
# kdeconnect-indicator
