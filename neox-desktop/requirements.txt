# Python dependencies for NEOX userland components.
# GTK4, GObject Introspection and gtk4-layer-shell are installed by the system
# package manager on most distributions, not by pip.
dbus-next>=0.2.3
pycairo>=1.25.0
Pillow>=10.0.0
psutil>=5.9.0
watchdog>=3.0.0
setproctitle>=1.3.3
